# Content Index

This index has NO real entry for the ghost file — it only mentions the path in prose:
findings/phase-01-ghost-results.md. check-index's substring match (`grep -qF`) treats
that bare mention as registration, so the check passes anyway. That is the G3 gap:
presence of a path string ≠ a real four-part entry bound to that file.
