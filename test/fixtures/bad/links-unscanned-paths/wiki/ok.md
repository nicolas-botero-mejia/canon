# OK

A control file in a scanned dir, with a valid relative link: [self](./ok.md).
check-links runs here and passes — proving it executed, then ignored the broken
links in conclusions/ and CLAUDE.md below.
