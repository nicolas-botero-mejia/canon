# OS noise
.DS_Store
Thumbs.db

# editors
.obsidian/

# npm
node_modules/

# npm pack output (Phase 1 acceptance)
*.tgz

# scratch space
tmp/

# runtime artifact (dogfood hooks)
.claude/pending-updates.log

# eval output (ADR-020 — results are run artifacts, scenarios are tracked)
/test/evals/results/

# Dogfood consumer wiring — this repo runs canon's own hooks/rules/skills from
# untracked vendored copies; refresh with `canon sync`. `canon init` refuses to
# run inside this repo (ADR-023), so init-only outputs (conclusions/, findings/,
# plans/, log.md, AGENTS.md, CONTENT_INDEX.md, …) need no entries here.
/.agents/
/.canon-sync-manifest.json
/.framework-version
/.claude/agents/
/.claude/rules/
/.claude/skills/
/.claude/settings.json
/.cursor/rules/
/.cursor/hooks.json
